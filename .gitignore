# SPDX-License-Identifier: Apache-2.0

# ── Python ──────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.egg-info/
dist/
build/
.venv/
venv/
*.whl

# ── Docker ──────────────────────────────────────────────
.env
docker-compose.override.yml

# ── Forensic artifacts (never commit evidence) ──────────
/forensics/
*.qcow2
*.zst
*.pcap
*.pcapng
*.memdump
evidence_manifest.jsonl

# ── IDE / Editor ────────────────────────────────────────
.vscode/
.idea/
*.swp
*.swo
*~

# ── OS generated ────────────────────────────────────────
.DS_Store
Thumbs.db
Desktop.ini
ehthumbs.db

# ── Secrets (never commit credentials) ─────────────────
*.pem
*.key
.minio-password
.env.local
.env.production

# ── Build artifacts ─────────────────────────────────────
*.o
*.so
*.a
linux-5.15.162/

# ── LaTeX build artifacts ───────────────────────────────
*.aux
*.log
*.bbl
*.blg
*.fls
*.fdb_latexmk
*.synctex.gz
*.out

# ── Volatility ISF (large JSON, downloaded via CI) ─────
*.isf.json

# ── Claude Code local state ────────────────────────────
.claude/
